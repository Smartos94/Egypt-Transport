package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ==================== PREMIUM COLOR SYSTEM ====================

// Luxury Obsidian Dark Mode Palette
val ObsidianDarkBg = Color(0xFF0C1014)       // Deep luxury black with greenish-slate undertones
val DarkSurfaceCardBg = Color(0xFF151D18)    // Dark velvet green-slate card background
val DarkOutlineBorder = Color(0xFF222F26)    // Sleek border for cards in dark mode

// Modern High-Contrast Light Mode Palette
val PristineLightBg = Color(0xFFF3F7F2)      // Soft clean light cream-green backdrop (matches reference image)
val LightSurfaceCardBg = Color(0xFFFFFFFF)   // Pure white for cards (pop layout)
val LightOutlineBorder = Color(0xFFE2EBE0)   // Clean modern light borders with soft green tint

// Egyptian Branding & Line Accent Colors
val PharaonicGoldDark = Color(0xFF5B9D0B)   // Rich high-contrast lime-green for light mode (highly visible and readable)
val PharaonicGold = Color(0xFF9FE514)       // Bright neon premium lime for dark mode glowing accents
val PharaonicGoldLight = Color(0xFFECFCCB)  // Light neon-lime chip background (matches reference card)

// Monorail Line System Colors
val EastNileCrimson = Color(0xFF5B9D0B)      // Lime-green for East Nile (matches app primary green interface)
val EastNileCrimsonLight = Color(0xFFECFCCB) // Light neon-lime for chips/borders
val WestNileEmerald = Color(0xFF22C55E)      // Emerald green for West Nile (Modern Green, as is)
val WestNileEmeraldLight = Color(0xFFDCFCE7) // Light green for chips/borders

// Typographic High-Contrast Primary & Secondary Text
val DarkTextPrimary = Color(0xFFF8FAFC)      // Contrast white text
val DarkTextSecondary = Color(0xFF94A3B8)    // Cool slate text for descriptions
val LightTextPrimary = Color(0xFF0F172A)     // Dark charcoal text for ultimate premium legibility
val LightTextSecondary = Color(0xFF475569)   // Slate gray text for descriptions

